# ngx-charts@24 déclare @angular/platform-browser-dynamic en peerDependency,
# ce qui crée un faux conflit avec Angular 20.3 en résolution stricte.
# legacy-peer-deps assouplit la résolution (installation + build déploiement).
legacy-peer-deps=true
